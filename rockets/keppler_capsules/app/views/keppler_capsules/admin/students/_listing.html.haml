- objects.each do |student|
  %tr{ id: "row-#{student.id}" }
    %td.item-check{ style: 'width: 10%' }
      - if can?(KepplerCapsules::Student).destroy_multiple?
        .checkbox
          %label{ for: "checkbox-#{student.id}" }
            %input{ type:"checkbox", id: "checkbox-#{student.id}", 'checklist-model' => 'check', 'checklist-value' => "#{student.id}", '@click' => "listDelete(#{student.id})"}
            %span.cr
              %i.cr-icon.glyphicon.glyphicon-ok
    %td.hidden-xs
      = link_to admin_capsules_student_path(student, search: @query), remote: true, class: 'show-row' do
        %span
          = student.name
    %td{ style: 'width: 15px;padding-right:15px;' }
      %a.action-btn.pull-right{:id => "menu-#{student.id}", "aria-expanded" => "false", "aria-haspopup" => "true", "data-toggle" => "dropdown"}
        %i.icon-options-vertical
      %ul.actions.dropdown-menu{"aria-labelledby" => "menu-#{student.id}" }
        - if can?(KepplerCapsules::Student).index?
          %li
            = link_to admin_capsules_student_path(student), class: 'new-menu', title: t('keppler.actions.show') do
              %i.icon-eye
              = t('keppler.actions.show')
        - if can?(KepplerCapsules::Student).update?
          %li
            = link_to edit_admin_capsules_student_path(student), class: 'new-menu', title: t('keppler.actions.edit') do
              %i.icon-pencil
              = t('keppler.actions.edit')
        - if can?(KepplerCapsules::Student).clone?
          %li
            = link_to admin_capsules_student_clone_path(student), class: 'new-menu', title: t('keppler.actions.clone') do
              %i.icon-docs
              = t('keppler.actions.clone')
        - if can?(KepplerCapsules::Student).destroy?
          %li
            = link_to admin_capsules_student_path(student), class: 'new-menu', title: t('keppler.actions.delete'), method: :delete, data: { confirm: t("keppler.messages.data_confirm") } do
              %i.icon-trash
              = t('keppler.actions.delete')

    %td{:style => 'width: 5%;'}
      %a.drop{:href=>"#"}
        %i.icon-cursor-move
