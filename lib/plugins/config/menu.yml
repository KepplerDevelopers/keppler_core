menu:
  keppler_rocket:
    name: keppler menu
    current: ['/']
    url_path: /
    icon: rocket
    model: User
    environment: all
    position: 4
    submenu:
      # - new:
      #     name: + Add new module
      #     url_path: /url_path
      #     current: ['/current_path']
      #     enviroment: development
      #     model: User
