.listing-show-header#header-category
	.actions
		- if can? :update, model
			= link_to edit_category_path(@category), class: "btn-floating waves-effect waves-light btn-flat tooltipped", "data-position" => "bottom", "data-tooltip" => "Editar" do
				= material_icon.md_24.create
	%h5
		= material_icon.md_24.account_circle.css_class("md-light")
		= t("model.singular.#{controller_name.singularize}")
.listing-show-body.card#body-category{ resize: true , style: "height: {{windowHeight}}px;" }
	.card-content
		%ul.list
			%li.list-row
				.display-label= model.human_attribute_name(:id)
				.display-value= @category.id
			%li.list-row
				.display-label= model.human_attribute_name(:name)
				.display-value= @category.name
			%li.list-row
				.display-label= model.human_attribute_name(:permalink)
				.display-value= @category.permalink
			- unless @category.subcategories.empty?
				%li.list-row
					.display-label= t('activerecord.attributes.keppler_blog/category.subcategories')
					.display-value.subcategory
						- @category.subcategories.each do |subcategory|
							%span.subcategory= subcategory.name
			%li.list-row
				= link_to t('keppler.actions.back_history'), categories_path


:javascript
	$('.tooltipped').tooltip({delay: 1});