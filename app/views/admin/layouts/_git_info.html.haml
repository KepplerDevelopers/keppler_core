- if Rails.env.eql?('development') && @git.installed?
  .current_branch{title: @git.changes_count[:changes]}
    %b    
      %i.bx.bx-git-branch 
      Branch: 
    = @git.current_branch
    .git-commit-status{style: "background-color: #{@git.changes_count[:color]}"}