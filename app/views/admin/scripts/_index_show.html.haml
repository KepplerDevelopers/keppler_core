
%aside.show-sidebar.control-sidebar.control-sidebar-light
  .tab-content
  %p.control-sidebar-heading
    = t("keppler.models.singularize.#{controller_name.singularize}")
    - if can? :update, Script
      = link_to edit_admin_script_path(@script), class: "btn-floating waves-effect waves-light btn-flat tooltipped", "data-position" => "bottom", "data-tooltip" => t("keppler.actions.edit") do
        %i.icon-pencil
  %table.table
    %tr.list-row
      %th.display-label= t("activerecord.attributes.name")
    %tr.list-row
      %td.display-value= @script.name
    %tr.list-row
      %th.display-label= t("activerecord.attributes.script")
    %tr.list-row
      %td.display-value= @script.script
    %tr.list-row
      %th.display-label= t("activerecord.attributes.url")
    %tr.list-row
      %td.display-value
        = link_to @script.url, @script.url, target: "_blank"
  %table.table
    %tr.list-row
      %th= link_to t('keppler.actions.back_history'), admin_scripts_path
